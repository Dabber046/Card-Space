{
    "name": "pokemon-fullstack-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "render-build": "cd client && npm ci && npx vite build"
    },
    "dependencies": {
        "mongodb": "^6.15.0"
    }
}
